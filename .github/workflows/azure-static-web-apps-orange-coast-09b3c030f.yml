name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job

    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false

      # 👇 Node setup for Vite build
      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      # 👇 Python setup for Function App
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      # 👇 Install Python dependencies
      - name: Install API dependencies
        run: |
          cd api
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # 👇 Build and Deploy to Azure Static Web Apps
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_ORANGE_COAST_09B3C030F }}
          action: "upload"
          app_location: "/"          # React app root
          api_location: "api"        # Python API folder
          output_location: "dist"    # Vite build output